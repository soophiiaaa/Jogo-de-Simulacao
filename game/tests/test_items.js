const { Items, Food, FirstAid, Survival } = require('./items')

const food = new Food()
const firstaid = new FirstAid()
const survival = new Survival()

console.log(food)
console.log(firstaid)
console.log(survival)
